extends layout

block content
  h1= title
  p Welcome to your #[em Inventory], a very basic Express website developed as an assignment for The Odin Project.

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The library has the following record counts:

    ul
      li #[strong Categories:] !{data.category_count}
      li #[strong Items:] !{data.item_count}

